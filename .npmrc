# pnpm config for the workspace.
# Downgraded packageManager to pnpm 9.15.4 (see root package.json) so
# the strict-dep-builds policy that pnpm 11 introduced doesn't apply
# at all. These flags are kept for documentation and future-proofing.
auto-install-peers=false
strict-peer-dependencies=false
