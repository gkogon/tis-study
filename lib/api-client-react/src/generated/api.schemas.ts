/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface ErrorResponse {
  error: string;
}

export interface TrafficFlowHistorySnapshot {
  capturedAt: string;
  incidentCount: number;
  snappedCount: number;
  weatherCondition?: string | null;
}

export interface TrafficFlowHistorySummary {
  totalSnapshots: number;
  earliestCapturedAt?: string | null;
  latestCapturedAt?: string | null;
  windowSnapshots: number;
  windowAvgIncidents: number;
  windowMaxIncidents: number;
}

export interface TrafficFlowHistory {
  summary: TrafficFlowHistorySummary;
  series: TrafficFlowHistorySnapshot[];
}

export interface SignalTiming {
  nsGreen: number;
  ewGreen: number;
  protectedLeft: number;
  allRed: number;
  cycleLength: number;
}

export interface TrafficVolume {
  north: number;
  south: number;
  east: number;
  west: number;
  total: number;
}

/**
 * Three turning movements from a single approach (incoming direction).
 */
export interface ApproachMovements {
  thru: number;
  left: number;
  right: number;
}

/**
 * Full 12-movement breakdown (4 approaches × thru/left/right) with totals.
 */
export interface MovementBreakdown {
  northbound: ApproachMovements;
  southbound: ApproachMovements;
  eastbound: ApproachMovements;
  westbound: ApproachMovements;
  totalThrough: number;
  totalLeft: number;
  totalRight: number;
  /** Left + right turns combined. */
  totalTurning: number;
  total: number;
}

export type WorstMovementPhase =
  (typeof WorstMovementPhase)[keyof typeof WorstMovementPhase];

export const WorstMovementPhase = {
  Protected_Left: "Protected Left",
  "N/S_Green": "N/S Green",
  "E/W_Green": "E/W Green",
} as const;

export interface WorstMovement {
  label: string;
  phase: WorstMovementPhase;
  vcRatio: number;
}

export interface RoadNetwork {
  classes: string[];
  ways: unknown[];
}

export type IntersectionSummaryRoadClass =
  (typeof IntersectionSummaryRoadClass)[keyof typeof IntersectionSummaryRoadClass];

export const IntersectionSummaryRoadClass = {
  motorway: "motorway",
  trunk: "trunk",
  primary: "primary",
  secondary: "secondary",
  other: "other",
} as const;

export type IntersectionSummarySeverity =
  (typeof IntersectionSummarySeverity)[keyof typeof IntersectionSummarySeverity];

export const IntersectionSummarySeverity = {
  low: "low",
  moderate: "moderate",
  high: "high",
  critical: "critical",
} as const;

export interface IntersectionSummary {
  id: string;
  name: string;
  zone: string;
  roadClass: IntersectionSummaryRoadClass;
  latitude: number;
  longitude: number;
  totalVolume: number;
  /** Total turning movements (left + right) per hour at this intersection. */
  turningVolume: number;
  inefficiencyScore: number;
  avgDelaySeconds: number;
  severity: IntersectionSummarySeverity;
}

export type IntersectionRoadClass =
  (typeof IntersectionRoadClass)[keyof typeof IntersectionRoadClass];

export const IntersectionRoadClass = {
  motorway: "motorway",
  trunk: "trunk",
  primary: "primary",
  secondary: "secondary",
  other: "other",
} as const;

export type IntersectionSeverity =
  (typeof IntersectionSeverity)[keyof typeof IntersectionSeverity];

export const IntersectionSeverity = {
  low: "low",
  moderate: "moderate",
  high: "high",
  critical: "critical",
} as const;

export interface Intersection {
  id: string;
  name: string;
  zone: string;
  roadClass: IntersectionRoadClass;
  latitude: number;
  longitude: number;
  totalVolume: number;
  turningVolume: number;
  signalTiming: SignalTiming;
  trafficVolume: TrafficVolume;
  movementBreakdown: MovementBreakdown;
  worstMovement: WorstMovement;
  inefficiencyScore: number;
  avgDelaySeconds: number;
  severity: IntersectionSeverity;
}

export interface HourlyLoadPoint {
  hour: number;
  label: string;
  throughVehicles: number;
  /** Combined left + right turning vehicles for the hour. */
  turningVehicles: number;
  congestionIndex: number;
}

export type RecommendationTargetPhase =
  (typeof RecommendationTargetPhase)[keyof typeof RecommendationTargetPhase];

export const RecommendationTargetPhase = {
  Protected_Left: "Protected Left",
  "N/S_Green": "N/S Green",
  "E/W_Green": "E/W Green",
} as const;

export interface Recommendation {
  intersectionId: string;
  intersectionName: string;
  targetPhase: RecommendationTargetPhase;
  /** Human-readable label for the bottleneck movement (e.g. "N/S Through"). */
  targetMovement: string;
  currentPhaseSeconds: number;
  suggestedPhaseSeconds: number;
  currentCycleLength: number;
  suggestedCycleLength: number;
  estimatedDelayReductionSeconds: number;
  rationale: string;
}

export interface IntersectionDetail {
  intersection: Intersection;
  hourlyLoad: HourlyLoadPoint[];
  recommendation: Recommendation;
}

export interface TrafficSummary {
  intersectionCount: number;
  totalDailyVehicles: number;
  /** Sum of left + right turning movements per hour across all intersections. */
  totalTurningMovementsPerHour: number;
  totalThroughMovementsPerHour: number;
  averageInefficiency: number;
  averageDelaySeconds: number;
  criticalCount: number;
  highCount: number;
  moderateCount: number;
  lowCount: number;
  worstIntersectionId: string;
  worstIntersectionName: string;
  worstIntersectionScore: number;
}

export type InefficiencyRankingSeverity =
  (typeof InefficiencyRankingSeverity)[keyof typeof InefficiencyRankingSeverity];

export const InefficiencyRankingSeverity = {
  low: "low",
  moderate: "moderate",
  high: "high",
  critical: "critical",
} as const;

export interface InefficiencyRanking {
  id: string;
  name: string;
  zone: string;
  inefficiencyScore: number;
  avgDelaySeconds: number;
  turningVolume: number;
  worstMovement: string;
  protectedLeftSeconds: number;
  severity: InefficiencyRankingSeverity;
}

export interface ZoneStat {
  zone: string;
  intersectionCount: number;
  avgInefficiency: number;
  totalTurningMovements: number;
  totalVolume: number;
  avgDelaySeconds: number;
}

export type MovementTypeStatMovement =
  (typeof MovementTypeStatMovement)[keyof typeof MovementTypeStatMovement];

export const MovementTypeStatMovement = {
  through: "through",
  left: "left",
  right: "right",
} as const;

export interface MovementTypeStat {
  movement: MovementTypeStatMovement;
  label: string;
  totalVehicles: number;
  avgPerIntersection: number;
}

export interface SignalTimingPoint {
  id: string;
  name: string;
  nsGreen: number;
  ewGreen: number;
  protectedLeft: number;
  allRed: number;
  cycleLength: number;
}

/**
 * Side-by-side before/after metrics for the optimization scenario.
 */
export interface BeforeAfterComparison {
  avgInefficiencyBefore: number;
  avgInefficiencyAfter: number;
  avgDelayBefore: number;
  avgDelayAfter: number;
  criticalBefore: number;
  criticalAfter: number;
  highBefore: number;
  highAfter: number;
  moderateBefore: number;
  moderateAfter: number;
  lowBefore: number;
  lowAfter: number;
  worstScoreBefore: number;
  worstScoreAfter: number;
  /** Count of signals that moved out of the high or critical severity bands. */
  bottlenecksEliminated: number;
  /** Average percent reduction in the worst-movement v/c ratio across all signals. */
  avgWorstVcReductionPct: number;
}

export type IntersectionChangeSeverityBefore =
  (typeof IntersectionChangeSeverityBefore)[keyof typeof IntersectionChangeSeverityBefore];

export const IntersectionChangeSeverityBefore = {
  low: "low",
  moderate: "moderate",
  high: "high",
  critical: "critical",
} as const;

export type IntersectionChangeSeverityAfter =
  (typeof IntersectionChangeSeverityAfter)[keyof typeof IntersectionChangeSeverityAfter];

export const IntersectionChangeSeverityAfter = {
  low: "low",
  moderate: "moderate",
  high: "high",
  critical: "critical",
} as const;

/**
 * Per-signal before/after diff used to render the optimization map.
 */
export interface IntersectionChange {
  id: string;
  latitude: number;
  longitude: number;
  /** Used for marker sizing. */
  totalVolume: number;
  severityBefore: IntersectionChangeSeverityBefore;
  severityAfter: IntersectionChangeSeverityAfter;
  scoreBefore: number;
  scoreAfter: number;
  worstVcBefore: number;
  worstVcAfter: number;
  worstMovementBefore: string;
  worstMovementAfter: string;
  nsGreenBefore: number;
  nsGreenAfter: number;
  ewGreenBefore: number;
  ewGreenAfter: number;
  protectedLeftBefore: number;
  protectedLeftAfter: number;
  cycleLengthBefore: number;
  cycleLengthAfter: number;
}

/**
 * Full dashboard bundle re-computed with all signals retimed to their algorithmic suggestion.
 */
export interface OptimizedScenario {
  summary: TrafficSummary;
  intersections: IntersectionSummary[];
  signalChanges: IntersectionChange[];
  rankings: InefficiencyRanking[];
  turnDistribution: MovementTypeStat[];
  zoneBreakdown: ZoneStat[];
  signalTiming: SignalTimingPoint[];
  peakHourLoad: HourlyLoadPoint[];
  comparison: BeforeAfterComparison;
}

/**
 * Provenance for the accident dataset baked into the API.
 */
export interface AccidentRiskMeta {
  sourceCollisions: string;
  sourceFARS: string;
  dateRange: string;
  /** Total raw crash records in the source dataset. */
  totalCrashes: number;
  /** Crashes successfully snapped to a signalized intersection within the snap radius. */
  snappedCrashes: number;
  snappedPct: number;
  signalsWithData: number;
  snapRadiusMeters: number;
  farsRecords: number;
}

export type SignalAccidentRiskRiskTier =
  (typeof SignalAccidentRiskRiskTier)[keyof typeof SignalAccidentRiskRiskTier];

export const SignalAccidentRiskRiskTier = {
  none: "none",
  low: "low",
  moderate: "moderate",
  high: "high",
  severe: "severe",
} as const;

/**
 * Historical crash aggregate snapped to a single signalized intersection.
 */
export interface SignalAccidentRisk {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
  /** Total reported crashes 2019-2023 within the snap radius. */
  crashes: number;
  fatal: number;
  seriousInjuries: number;
  /** Sum of ARC severity scores (higher = worse). */
  severityScore: number;
  riskTier: SignalAccidentRiskRiskTier;
}

export interface AccidentRiskBundle {
  meta: AccidentRiskMeta;
  perSignal: SignalAccidentRisk[];
  /** 24-element hour-of-day distribution (share of crashes by hour). */
  hourly: number[];
  /** 7-element day-of-week distribution (Sun..Sat). */
  dow: number[];
  /** 12-element month-of-year distribution (Jan..Dec). */
  monthly: number[];
}

/**
 * Optional special-event toggles that bias the prediction near specific corridors.
 */
export interface EventFlags {
  /** Falcons home game at Mercedes-Benz Stadium — boosts I-75/I-85 connector + Northside/Marietta corridors. */
  falconsHome?: boolean;
  /** Hawks home game at State Farm Arena — same downtown corridor footprint as Falcons. */
  hawksHome?: boolean;
  /** Braves home game at Truist Park (Cobb) — boosts I-285 NW + Cumberland. */
  bravesHome?: boolean;
  /** Georgia Tech football — boosts North Avenue + 10th/14th midtown corridor. */
  gtFootball?: boolean;
  /** Federal holiday — flattens commute peaks, shifts to leisure pattern. */
  holiday?: boolean;
  /** Public-school day in session — adds AM 7-8a + PM 2:30-3:30p school zones. */
  schoolDay?: boolean;
}

export type PredictDayRequestWeather =
  (typeof PredictDayRequestWeather)[keyof typeof PredictDayRequestWeather];

export const PredictDayRequestWeather = {
  clear: "clear",
  light_rain: "light_rain",
  heavy_rain: "heavy_rain",
  snow: "snow",
} as const;

export interface PredictDayRequest {
  /** Target date in YYYY-MM-DD form (interpreted in America/New_York). */
  date: string;
  weather?: PredictDayRequestWeather;
  events?: EventFlags;
}

export type PredictedSignalPredictedSeverity =
  (typeof PredictedSignalPredictedSeverity)[keyof typeof PredictedSignalPredictedSeverity];

export const PredictedSignalPredictedSeverity = {
  low: "low",
  moderate: "moderate",
  high: "high",
  critical: "critical",
} as const;

export type PredictedSignalCrashRiskTier =
  (typeof PredictedSignalCrashRiskTier)[keyof typeof PredictedSignalCrashRiskTier];

export const PredictedSignalCrashRiskTier = {
  none: "none",
  low: "low",
  moderate: "moderate",
  high: "high",
  severe: "severe",
} as const;

/**
 * Forecast for a single signal across the chosen day.
 */
export interface PredictedSignal {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
  roadClass: string;
  /** Inefficiency score under nominal peak conditions (the existing dashboard's value). */
  baselineScore: number;
  /** Worst hourly score expected on the target day. */
  predictedPeakScore: number;
  /** Hour 0-23 at which the worst score occurs. */
  predictedPeakHour: number;
  predictedAvgScore: number;
  predictedSeverity: PredictedSignalPredictedSeverity;
  /** predictedPeakScore - baselineScore. */
  deltaVsBaseline: number;
  crashRiskTier: PredictedSignalCrashRiskTier;
  /** Extra delay/score multiplier (as a percentage) applied for accident-prone signals. */
  crashSurgePct: number;
  /** 24 predicted scores, one per hour 0..23. */
  hourly: number[];
}

export interface PredictedDayMeta {
  date: string;
  dayOfWeek: string;
  weather: string;
  events?: EventFlags;
  dowMultiplier: number;
  weatherMultiplier: number;
  eventMultiplier: number;
  criticalCount: number;
  highCount: number;
  moderateCount: number;
  lowCount: number;
  avgPredictedScore: number;
  /** The hour with the highest network-wide aggregate predicted score. */
  peakHourGlobal: number;
}

export interface HourlyAggregatePoint {
  hour: number;
  label: string;
  avgPredictedScore: number;
  criticalCount: number;
  highCount: number;
  weatherFactor: number;
  /** Hour-of-day crash likelihood factor (FARS-derived). */
  crashLikelihoodFactor: number;
}

export interface PreemptiveRecommendation {
  intersectionId: string;
  intersectionName: string;
  latitude: number;
  longitude: number;
  predictedPeakHour: number;
  predictedPeakScore: number;
  /** Plain-English suggested operator action for the predicted peak window. */
  action: string;
  rationale: string;
}

export interface PredictedDay {
  meta: PredictedDayMeta;
  hourly: HourlyAggregatePoint[];
  /** Lean per-signal forecast for every signal in the network (used for the predicted heatmap). */
  signals: PredictedSignal[];
  /** Top-30 predicted-worst signals sorted by predictedPeakScore. */
  topWorst: PredictedSignal[];
  /** Pre-emptive operator recommendations for the worst predicted bottlenecks. */
  recommendations: PreemptiveRecommendation[];
}

export type LiveIncidentCategory =
  (typeof LiveIncidentCategory)[keyof typeof LiveIncidentCategory];

export const LiveIncidentCategory = {
  Incidents: "Incidents",
  IncidentClosures: "IncidentClosures",
} as const;

/**
 * A single active incident pulled from GDOT 511 and snapped to the nearest signal within 500m (signalId is null if no signal is close enough).
 */
export interface LiveIncident {
  id: string;
  source: string;
  description: string;
  roadway: string;
  eventType: string;
  eventSubType: string;
  severity: string;
  isFullClosure: boolean;
  laneDescription: string;
  startDate: string | null;
  endDate: string | null;
  lastUpdated: string | null;
  latitude: number;
  longitude: number;
  signalId: string | null;
  signalName: string | null;
  signalDistanceM: number | null;
  category: LiveIncidentCategory;
}

export interface LiveIncidentsBundle {
  fetchedAt: string;
  source: string;
  cached: boolean;
  cacheAgeSeconds: number;
  /** Total icons fetched across all categories before bbox/dedupe filtering. */
  totalFetched: number;
  /** Unique incidents inside the Atlanta metro bbox after dedupe. */
  inMetro: number;
  /** How many of those incidents fell within 500m of a known signal. */
  snappedToSignal: number;
  incidents: LiveIncident[];
}

/**
 * Map of OSM signal id (as string) to the number of incidents observed on that signal today.
 */
export type DaySnapshotActualIncidentSignalCounts = { [key: string]: number };

/**
 * One day's predicted-vs-actual record persisted in the rolling history file.
 */
export interface DaySnapshot {
  /** YYYY-MM-DD in America/New_York. */
  date: string;
  observedAt: string;
  predictedTopRiskOsmIds: number[];
  topNUsed: number;
  /** Map of OSM signal id (as string) to the number of incidents observed on that signal today. */
  actualIncidentSignalCounts: DaySnapshotActualIncidentSignalCounts;
  actualIncidentTotal: number;
  /** How many actually-incident signals appeared in the predicted top-N. */
  hits: number;
  /** hits divided by */
  hitRatePct: number;
  /** hits divided by topN — how concentrated the model's top-N actually was. */
  precisionPct: number;
}

export interface AccuracyReport {
  today?: DaySnapshot | null;
  /** Up to 7 most recent prior days' snapshots (newest first). */
  recentDays: DaySnapshot[];
  rollingHitRatePct: number;
  rollingPrecisionPct: number;
  liveIncidentsTotal: number;
  liveIncidentsSnapped: number;
  /** Number of distinct signals with a recent-activity multiplier currently being applied to future predictions. */
  recentActivitySignalCount: number;
  /** True when the public 511ga.org feed returned zero items (treated as an outage); today's snapshot was NOT persisted in this case. */
  upstreamDegraded: boolean;
}

/**
 * One day of the backtest series.
 */
export interface BacktestDayRow {
  date: string;
  /**
   * @minimum 0
   * @maximum 6
   */
  dow: number;
  topN: number;
  actualSnappedSignals: number;
  actualIncidentTotal: number;
  hits: number;
  hitRatePct: number;
  precisionPct: number;
}

export interface DowBreakdownRow {
  /**
   * @minimum 0
   * @maximum 6
   */
  dow: number;
  dayName: string;
  daysObserved: number;
  meanHitRatePct: number;
  meanPrecisionPct: number;
}

/**
 * Aggregated multi-day backtest report (model accuracy sales sheet).
 */
export interface BacktestReport {
  generatedAt: string;
  windowDays: number;
  totalSignalsInModel: number;
  daysObserved: number;
  totalPredictedSlots: number;
  totalObservedIncidentSignals: number;
  totalIncidentReports: number;
  totalHits: number;
  overallHitRatePct: number;
  overallPrecisionPct: number;
  meanHitRatePct: number;
  medianHitRatePct: number;
  hitRateCi95LowPct: number;
  hitRateCi95HighPct: number;
  meanTopN: number;
  randomBaselinePct: number;
  liftMultiplier: number;
  bestDay?: BacktestDayRow | null;
  worstDay?: BacktestDayRow | null;
  perDay: BacktestDayRow[];
  byDayOfWeek: DowBreakdownRow[];
  methodology: string[];
}

export type LiveWeatherHourlyCondition =
  (typeof LiveWeatherHourlyCondition)[keyof typeof LiveWeatherHourlyCondition];

export const LiveWeatherHourlyCondition = {
  clear: "clear",
  light_rain: "light_rain",
  heavy_rain: "heavy_rain",
  snow: "snow",
} as const;

export interface LiveWeatherHourly {
  /**
   * @minimum 0
   * @maximum 23
   */
  hour: number;
  condition: LiveWeatherHourlyCondition;
  summary: string;
  precipitationInPerHr: number;
  weatherCode: number;
}

export type LiveWeatherCondition =
  (typeof LiveWeatherCondition)[keyof typeof LiveWeatherCondition];

export const LiveWeatherCondition = {
  clear: "clear",
  light_rain: "light_rain",
  heavy_rain: "heavy_rain",
  snow: "snow",
} as const;

export interface LiveWeather {
  fetchedAt: string;
  source: string;
  cached: boolean;
  cacheAgeSeconds: number;
  condition: LiveWeatherCondition;
  summary: string;
  temperatureF?: number | null;
  precipitationInPerHr?: number | null;
  windMph?: number | null;
  weatherCode?: number | null;
  hourly: LiveWeatherHourly[];
}

export type AccidentHotspotCrashRiskTier =
  (typeof AccidentHotspotCrashRiskTier)[keyof typeof AccidentHotspotCrashRiskTier];

export const AccidentHotspotCrashRiskTier = {
  none: "none",
  low: "low",
  moderate: "moderate",
  high: "high",
  severe: "severe",
} as const;

export interface AccidentHotspot {
  rank: number;
  intersectionId: string;
  intersectionName: string;
  latitude: number;
  longitude: number;
  hour: number;
  hourLabel: string;
  /** Display-normalized 0..100 score, 100 = strongest candidate in this run. */
  likelihood: number;
  predictedScore: number;
  crashRiskTier: AccidentHotspotCrashRiskTier;
  historicalCrashes: number;
  isPast: boolean;
  rationale: string;
}

export type TodayAccidentPredictionsWeather =
  (typeof TodayAccidentPredictionsWeather)[keyof typeof TodayAccidentPredictionsWeather];

export const TodayAccidentPredictionsWeather = {
  clear: "clear",
  light_rain: "light_rain",
  heavy_rain: "heavy_rain",
  snow: "snow",
} as const;

export interface TodayAccidentPredictions {
  date: string;
  generatedAt: string;
  weather: TodayAccidentPredictionsWeather;
  weatherSummary: string;
  currentHourLocal: number;
  includesPastHours: boolean;
  topN: number;
  hotspots: AccidentHotspot[];
}

export type LiveTrafficSegmentLevel =
  (typeof LiveTrafficSegmentLevel)[keyof typeof LiveTrafficSegmentLevel];

export const LiveTrafficSegmentLevel = {
  free: "free",
  light: "light",
  heavy: "heavy",
  severe: "severe",
  closed: "closed",
} as const;

export interface LiveTrafficSegment {
  /** OSM road class index (0=motorway, 1=trunk, 2=primary, 3=secondary). */
  rc: number;
  name: string | null;
  level: LiveTrafficSegmentLevel;
  /** Estimated congestion score 0..100. */
  score: number;
  /** Flat lat/lon pairs [lat0, lon0, lat1, lon1, ...]. */
  polyline: number[];
}

export type LiveTrafficFlowWeather =
  (typeof LiveTrafficFlowWeather)[keyof typeof LiveTrafficFlowWeather];

export const LiveTrafficFlowWeather = {
  clear: "clear",
  light_rain: "light_rain",
  heavy_rain: "heavy_rain",
  snow: "snow",
} as const;

export type LiveTrafficFlowCounts = {
  free: number;
  light: number;
  heavy: number;
  severe: number;
  closed: number;
};

export interface LiveTrafficFlow {
  fetchedAt: string;
  source: string;
  cached: boolean;
  cacheAgeSeconds: number;
  currentHourLocal: number;
  hourLabel: string;
  dayOfWeek: string;
  weather: LiveTrafficFlowWeather;
  weatherSummary: string;
  activeIncidents: number;
  fullClosures: number;
  counts: LiveTrafficFlowCounts;
  avgCongestion: number;
  classesIncluded: number[];
  segments: LiveTrafficSegment[];
  incidents: LiveIncident[];
  /** Number of DMS boards with parsed speed readings blended into the scores (0 if no API key). */
  dmsSpeedCount: number;
  sourceNote: string;
}

export interface DmsSpeedReading {
  id: string;
  name: string;
  roadway: string;
  direction: string;
  latitude: number;
  longitude: number;
  speedMph: number;
  rawMessage: string;
  lastUpdated: string | null;
}

export interface DmsSpeedBundle {
  fetchedAt: string;
  cached: boolean;
  cacheAgeSeconds: number;
  totalSigns: number;
  metroSigns: number;
  speedReadings: DmsSpeedReading[];
}

export interface GdotAlert {
  id: number;
  message: string;
  notes: string;
  startTime?: string | null;
  endTime?: string | null;
  lastUpdated?: string | null;
  highImportance: boolean;
}

export interface GdotAlertsBundle {
  fetchedAt: string;
  cached: boolean;
  cacheAgeSeconds: number;
  alerts: GdotAlert[];
}

export type LiveHotspotFixCurrentLevel =
  (typeof LiveHotspotFixCurrentLevel)[keyof typeof LiveHotspotFixCurrentLevel];

export const LiveHotspotFixCurrentLevel = {
  free: "free",
  light: "light",
  heavy: "heavy",
  severe: "severe",
  closed: "closed",
} as const;

export type LiveHotspotFixRiskTier =
  (typeof LiveHotspotFixRiskTier)[keyof typeof LiveHotspotFixRiskTier];

export const LiveHotspotFixRiskTier = {
  none: "none",
  low: "low",
  moderate: "moderate",
  high: "high",
  severe: "severe",
} as const;

export type LiveHotspotFixTargetPhase =
  (typeof LiveHotspotFixTargetPhase)[keyof typeof LiveHotspotFixTargetPhase];

export const LiveHotspotFixTargetPhase = {
  Protected_Left: "Protected Left",
  "N/S_Green": "N/S Green",
  "E/W_Green": "E/W Green",
} as const;

/**
 * A crash-prone intersection currently in heavy or worse congestion, with a concrete signal-timing recommendation derived from the same Webster's-delay engine the dashboard uses.
 */
export interface LiveHotspotFix {
  intersectionId: string;
  intersectionName: string;
  latitude: number;
  longitude: number;
  /** Current-hour predicted stress (0..100). */
  currentScore: number;
  currentLevel: LiveHotspotFixCurrentLevel;
  /** True iff an active GDOT incident sits within ~600m of the intersection. */
  hasNearbyIncident: boolean;
  nearestIncidentMeters: number | null;
  crashes: number;
  fatal: number;
  seriousInjuries: number;
  riskTier: LiveHotspotFixRiskTier;
  targetPhase: LiveHotspotFixTargetPhase;
  targetMovement: string;
  currentPhaseSeconds: number;
  suggestedPhaseSeconds: number;
  currentCycleLength: number;
  suggestedCycleLength: number;
  estimatedDelayReductionSeconds: number;
  rationale: string;
  /** Combined sort key (0..100) blending current stress, crash count, fatality bonus, and live-incident proximity. */
  hotspotScore: number;
}

export interface LiveHotspotFixesBundle {
  fetchedAt: string;
  currentHourLocal: number;
  hourLabel: string;
  /** How many signals matched the (heavy+ congestion AND high+ crash tier) filter before the top-N cap. */
  totalCandidates: number;
  fixes: LiveHotspotFix[];
  note: string;
}

export type PredictedTrafficFlowWeather =
  (typeof PredictedTrafficFlowWeather)[keyof typeof PredictedTrafficFlowWeather];

export const PredictedTrafficFlowWeather = {
  clear: "clear",
  light_rain: "light_rain",
  heavy_rain: "heavy_rain",
  snow: "snow",
} as const;

export type PredictedTrafficFlowCounts = {
  free: number;
  light: number;
  heavy: number;
  severe: number;
  closed: number;
};

export interface PredictedCriticalSignal {
  id: string;
  name: string;
  lat: number;
  lon: number;
  score: number;
  severity: string;
  roadClass: string;
  crashRiskTier: string;
  crashSurgePct: number;
}

export interface PredictedHourAggregate {
  hour: number;
  label: string;
  avgScore: number;
  criticalCount: number;
  highCount: number;
}

export interface PredictedTrafficFlow {
  date: string;
  hour: number;
  hourLabel: string;
  dayOfWeek: string;
  weather: PredictedTrafficFlowWeather;
  counts: PredictedTrafficFlowCounts;
  avgCongestion: number;
  segments: LiveTrafficSegment[];
  criticalSignals: PredictedCriticalSignal[];
  allHours: PredictedHourAggregate[];
  sourceNote: string;
}

export type ParkingLotKind =
  (typeof ParkingLotKind)[keyof typeof ParkingLotKind];

export const ParkingLotKind = {
  surface: "surface",
  garage: "garage",
  "multi-storey": "multi-storey",
  underground: "underground",
  street: "street",
  other: "other",
} as const;

export type ParkingLotFee = (typeof ParkingLotFee)[keyof typeof ParkingLotFee];

export const ParkingLotFee = {
  free: "free",
  paid: "paid",
  unknown: "unknown",
} as const;

export type ParkingLotAccess =
  (typeof ParkingLotAccess)[keyof typeof ParkingLotAccess];

export const ParkingLotAccess = {
  public: "public",
  customers: "customers",
  permit: "permit",
  private: "private",
  unknown: "unknown",
} as const;

export interface ParkingLot {
  id: string;
  lat: number;
  lon: number;
  name: string | null;
  kind: ParkingLotKind;
  fee: ParkingLotFee;
  access: ParkingLotAccess;
  capacity: number;
  fillPct: number;
  spacesFree: number;
  /** Minutes until lot reaches 95% fill, or null if not within next 12h */
  etaMinutes: number | null;
  archetype: string;
  surgeFromVenue: string | null;
}

export interface ParkingForecastHour {
  hour: number;
  label: string;
  fillPct: number;
  spacesFree: number;
}

export type ParkingLotDetailNearestVenue = {
  name: string;
  distanceKm: number;
} | null;

export interface ParkingLotDetail {
  id: string;
  lat: number;
  lon: number;
  name: string | null;
  kind: string;
  fee: string;
  access: string;
  capacity: number;
  fillPct: number;
  spacesFree: number;
  etaMinutes: number | null;
  archetype: string;
  surgeFromVenue: string | null;
  hourly: ParkingForecastHour[];
  drivers: string[];
  nearestVenue: ParkingLotDetailNearestVenue;
}

export interface ParkingSurgeEvent {
  venue: string;
  lat: number;
  lon: number;
  affectedLots: number;
  affectedCapacity: number;
  fillPct: number;
}

export interface ParkingArchetypeBreakdown {
  lots: number;
  capacity: number;
  fillPct: number;
}

export type ParkingSummaryByArchetype = {
  [key: string]: ParkingArchetypeBreakdown;
};

export interface ParkingSummary {
  asOf: string;
  totalLots: number;
  totalCapacity: number;
  occupiedSpaces: number;
  fillPct: number;
  fullLots: number;
  nearFullLots: number;
  surgeEvents: ParkingSurgeEvent[];
  byArchetype: ParkingSummaryByArchetype;
  sourceNote: string;
}

export type GetTrafficFlowHistoryParams = {
  since?: string;
  until?: string;
  /**
   * @minimum 1
   * @maximum 2000
   */
  limit?: number;
};

export type GetPredictionAccuracyParams = {
  /**
   * @minimum 10
   * @maximum 1000
   */
  topN?: number;
};

export type GetLiveTrafficFlowParams = {
  /**
   * Comma-separated OSM road class indices to include (0=motorway, 1=trunk, 2=primary, 3=secondary). Default `0,1,2`.
   */
  classes?: string;
};

export type GetPredictedTrafficFlowParams = {
  /**
   * Date in YYYY-MM-DD format
   */
  date: string;
  /**
   * Hour of day 0-23
   * @minimum 0
   * @maximum 23
   */
  hour: number;
  weather?: GetPredictedTrafficFlowWeather;
  schoolDay?: boolean;
  falconsHome?: boolean;
  hawksHome?: boolean;
  bravesHome?: boolean;
  gtFootball?: boolean;
  holiday?: boolean;
  /**
   * Comma-separated OSM road class indices (0=motorway, 1=trunk, 2=primary, 3=secondary). Default `0,1,2`.
   */
  classes?: string;
};

export type GetPredictedTrafficFlowWeather =
  (typeof GetPredictedTrafficFlowWeather)[keyof typeof GetPredictedTrafficFlowWeather];

export const GetPredictedTrafficFlowWeather = {
  clear: "clear",
  light_rain: "light_rain",
  heavy_rain: "heavy_rain",
  snow: "snow",
} as const;

export type GetParkingSummaryParams = {
  /**
   * If true, force every venue's event-surge window on (demo mode).
   */
  forceSurge?: boolean;
};

export type ListParkingLotsParams = {
  forceSurge?: boolean;
  /**
   * Filter out lots smaller than this estimated capacity.
   * @minimum 0
   */
  minCapacity?: number;
  /**
   * Filter out lots already above this fill percentage.
   */
  maxFillPct?: number;
  archetype?: ListParkingLotsArchetype;
};

export type ListParkingLotsArchetype =
  (typeof ListParkingLotsArchetype)[keyof typeof ListParkingLotsArchetype];

export const ListParkingLotsArchetype = {
  commercial: "commercial",
  office: "office",
  event: "event",
  park_and_ride: "park_and_ride",
  university: "university",
  airport: "airport",
  mixed: "mixed",
} as const;

export type GetTodayAccidentPredictionsParams = {
  /**
   * @minimum 1
   * @maximum 100
   */
  topN?: number;
  includePastHours?: boolean;
};
